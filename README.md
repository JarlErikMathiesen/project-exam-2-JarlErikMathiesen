## Overview

Holidaze is a single-page application for browsing venues, viewing details, and making bookings. Authenticated users can manage their profile, and users registered as managers can create, update, and delete their venues. The app integrates with the Noroff v2 API and uses an API key via import.meta.env.VITE_API_KEY. Core API access is abstracted by apiFetch().
Key features
